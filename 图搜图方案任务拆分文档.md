# 图搜图方案任务拆分文档（Java + DJL + Redis Stack）

## 1. 目标与范围

- 目标：实现“以图搜图”，并返回图片对应业务属性信息（如名称、分类、标签、时间、来源等）。
- 技术栈：`Spring Boot` + `DJL`（生成 embedding）+ `Redis Stack`（向量检索）+ `MySQL`（属性信息）。
- 范围：仅覆盖后端能力建设，不包含前端复杂交互优化。

## 2. 总体架构

- 向量生成：`eladmin-system` 内新增 embedding 服务（DJL）。
- 向量存储/检索：Redis RediSearch 向量索引（`HNSW` + `COSINE`）。
- 属性存储：MySQL 业务表存图片属性，Redis 仅存检索必要字段与向量。
- 查询链路：查询图 -> 生成 embedding -> Redis TopK -> MySQL 批量补全属性 -> 返回结果。

## 3. 并行任务拆分（可分布执行）

### 包 A：数据模型与库表（后端同学 A）

- 输入：
  - 业务需要返回的属性字段清单（产品/运营确认）。
- 工作项：
  - 设计图片主表（或扩展已有表）与属性字段。
  - 设计 Redis Key 规范（如 `img:{imageId}`）与字段映射。
  - 定义图片状态字段（待处理/已向量化/失败）。
- 交付物：
  - MySQL DDL 脚本。
  - 字段字典文档（含必填、索引、枚举说明）。
- 验收标准：
  - 支持按 `imageId` 唯一定位。
  - 支持批量按 `imageId` 查询属性，单次 100 条内响应稳定。

### 包 B：Embedding 服务（后端同学 B）

- 输入：
  - 模型选型固定为 `OpenCLIP ViT-B-32`（512 维）。
- 工作项：
  - 在 `eladmin-system` 新增 `EmbeddingService`（DJL 推理）。
  - 实现图片字节流 -> `float[512]` 向量。
  - 统一预处理与归一化，确保入库/查询同流程。
  - 增加健康检查与模型预热。
- 交付物：
  - `EmbeddingService` 及单元测试。
  - 性能基线报告（CPU 下 QPS/耗时）。
- 验收标准：
  - 单图 embedding 正确输出 512 维。
  - 同一图片重复向量余弦相似度 >= 0.999（浮动允许）。

### 包 C：Redis 向量索引与操作层（后端同学 C）

- 输入：
  - 向量维度、距离函数、索引参数（512 / COSINE / HNSW）。
- 工作项：
  - 建立 RediSearch 索引（向量字段 + 基本过滤字段）。
  - 实现向量写入、更新、删除、TopK 查询封装。
  - 封装 `VectorSearchRepository`，避免业务层直接拼命令。
- 交付物：
  - Redis 建索引脚本与 Java Repository 实现。
  - 本地压测脚本（TopK=20/50/100）。
- 验收标准：
  - 10 万数据规模下 Top20 查询延迟满足目标（例如 P95 < 200ms，按机器资源可调整）。
  - 支持按状态或分类进行基础过滤。

### 包 D：业务接口集成（后端同学 D）

- 输入：
  - 包 A/B/C 的接口与数据约定。
- 工作项：
  - 新增“图搜图”服务与控制器接口。
  - 查询流程：上传图片 -> embedding -> Redis 召回 -> MySQL 回表 -> DTO 返回。
  - 补充异常处理与降级（Redis/DJL 不可用时返回可解释错误码）。
- 交付物：
  - API 文档（请求/响应示例）。
  - 业务服务代码与集成测试。
- 验收标准：
  - API 可返回相似图片列表及属性信息。
  - 异常路径可观测（日志 + 明确错误码）。

### 包 E：数据导入与回填（数据同学 E）

- 输入：
  - 历史图片数据源、属性来源。
- 工作项：
  - 实现历史图片批处理（分批 embedding + 写 Redis + 更新状态）。
  - 实现失败重试与断点续跑。
  - 输出导入报表（总量、成功、失败、失败原因分布）。
- 交付物：
  - 导入脚本/任务配置与运行说明。
  - 首轮全量导入结果。
- 验收标准：
  - 全量导入成功率 >= 99%（可根据数据质量调整）。
  - 失败可重跑且无重复脏数据。

### 包 F：测试与验收（测试同学 F）

- 输入：
  - 完整接口与样例数据集（至少 1000 张）。
- 工作项：
  - 功能测试：检索正确性、属性完整性、边界输入。
  - 性能测试：并发查询、批量导入影响。
  - 稳定性测试：Redis 重启、模型服务超时、异常恢复。
- 交付物：
  - 测试用例清单与报告。
  - 上线准入建议。
- 验收标准：
  - 关键用例通过率 100%。
  - 性能与稳定性指标达成约定阈值。

## 4. 任务依赖关系

- 可并行启动：A、B、C、E、F（准备阶段）。
- 强依赖：
  - D 依赖 A/B/C 的接口稳定版本。
  - E 依赖 B/C。
  - F 依赖 D 的联调环境。
- 建议节奏：
  - 第 1 周：A/B/C 完成 PoC；E 准备数据。
  - 第 2 周：D 联调 + E 全量导入 + F 测试。
  - 第 3 周：性能调优、灰度、上线。

## 5. 接口与数据约定（基线）

- 向量维度：`512`
- 向量类型：`FLOAT32`
- 距离函数：`COSINE`
- TopK 默认：`20`
- 查询入参：
  - 图片文件（multipart）或图片 URL（二选一）
  - 可选过滤条件（分类、状态、时间区间）
- 查询返回：
  - `imageId`
  - `score`（相似度）
  - 属性字段（名称、分类、标签、URL、创建时间等）

## 6. 运行与运维要求

- 配置项：
  - DJL 模型路径/缓存目录
  - Redis 连接、索引名、TopK、超时
  - 开关项：`image.search.enabled`
- 可观测性：
  - 指标：embedding 耗时、Redis 查询耗时、回表耗时、总耗时、错误率
  - 日志：请求 traceId、召回数量、过滤条件、异常原因
- 容错策略：
  - Redis 不可用：快速失败并返回统一错误码
  - embedding 失败：记录失败样本，不写入脏向量

## 7. 风险清单与应对

- 模型效果不足：
  - 先抽样评估，再考虑切换到更强 CLIP 变体。
- Redis 内存压力：
  - 限制向量字段冗余；冷热分层；按业务分库或分索引。
- 数据质量差：
  - 增加图片有效性校验（格式、分辨率、重复图）。
- 查询延迟波动：
  - 调整 HNSW 参数，启用连接池与批量回表。

## 8. Definition of Done（最终完成标准）

- 以图搜图 API 在生产可用，返回图片及属性信息。
- 全量历史数据完成向量化并可检索。
- 关键性能指标满足约定阈值。
- 异常可观测、可追踪、可恢复。
- 文档齐全（部署、回滚、运维、测试报告）。

